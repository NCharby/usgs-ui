import {
    Container,
    CircularProgress
} from "@material-ui/core"


export default function InitialLoaderContainer(params) {
    
    return (
        <Container>
            <CircularProgress />
        </Container>
    )
}